import { VehiculeStatistics } from './vehicule-statistics';

describe('VehiculeStatistics', () => {
  it('should create an instance', () => {
    expect(new VehiculeStatistics()).toBeTruthy();
  });
});
